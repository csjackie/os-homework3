# oss execs child
# worker takes in -t (seconds and nanoseconds)
# worker attaches to shared memory
# start loop
	# check clock
	# termination = (current time + (-t input))
# end loop
# output message
